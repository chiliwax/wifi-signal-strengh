import { useContext } from 'react';
import { ThemeContext } from '../../contexts/theme.context';

export const use{{name}}Style = () => {
  const { colors } = useContext(ThemeContext);
  return { };
};